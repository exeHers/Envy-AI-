# Envy Personal Assistant - Requirements
# Core dependencies for local operation (no paid APIs required)

# Audio capture and processing
sounddevice>=0.4.6
numpy>=1.24.0
scipy>=1.10.0

# Wake word and speech recognition
vosk>=0.3.45
faster-whisper>=0.10.0  # Optimized whisper for CPU/GPU

# Text-to-speech
pyttsx3>=2.90
TTS>=0.22.0  # Coqui TTS (optional, higher quality)

# LLM support
llama-cpp-python>=0.2.0  # Local LLM inference
requests>=2.31.0  # For optional free remote endpoints
httpx>=0.25.0

# Web framework
fastapi>=0.104.0
uvicorn>=0.24.0
websockets>=12.0
jinja2>=3.1.2
python-multipart>=0.0.6

# Utilities
pyyaml>=6.0
python-dotenv>=1.0.0
psutil>=5.9.0
aiofiles>=23.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# System control
pyautogui>=0.9.54  # For system control skill

# Optional GPU acceleration (will install if CUDA available)
# onnxruntime-gpu>=1.16.0  # Uncomment if CUDA 11.x available
